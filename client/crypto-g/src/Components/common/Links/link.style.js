import styled from "styled-components";
import {colors} from "../../../styles-common/common.style";

export const LinkNeon = styled.a`
  cursor: pointer;
  font-size: 0.8rem;
  transition: .6s;
  :${colors.fireHover}
`;